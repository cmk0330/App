<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="196dp"
    android:background="?attr/colorPrimary"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_user_logo"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@mipmap/ic_user_photo"
        android:layout_marginTop="40dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/tv_user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="花开_"
        android:textColor="@color/white"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="@+id/iv_user_logo"
        app:layout_constraintStart_toStartOf="@+id/iv_user_logo"
        app:layout_constraintTop_toBottomOf="@+id/iv_user_logo" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="什么都没有留下~~"
        android:maxLines="2"
        android:textSize="14sp"
        android:textColor="@color/text_gray_light"
        app:layout_constraintEnd_toEndOf="@+id/iv_user_logo"
        app:layout_constraintStart_toStartOf="@+id/iv_user_logo"
        app:layout_constraintTop_toBottomOf="@+id/tv_user_name"
        android:layout_marginTop="12dp"
        tools:ignore="MissingConstraints" />

</androidx.constraintlayout.widget.ConstraintLayout>